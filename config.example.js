// -----------------------------------------------------------------------------
// TEMPLATE — copie ce fichier en config.js et remplis avec les valeurs
// de ton projet Supabase (Dashboard → Project Settings → API).
//
// config.js est listé dans .gitignore : tes clés ne seront jamais commit.
//
// Note sécurité :
// - La clé "anon public" est CONÇUE pour être exposée côté client, protégée
//   par tes règles Row Level Security. Ne partage jamais la clé "service_role".
// -----------------------------------------------------------------------------

window.SUPABASE_URL = "https://<TON-PROJET>.supabase.co";
window.SUPABASE_ANON_KEY = "<TA-CLE-ANON-PUBLIC>";
